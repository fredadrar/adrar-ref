{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Modifier la catégorie</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Valider" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('category_home') }}">Revenir sur la liste de catégories</a>
        </li>
    </ul>
{% endblock %}
